<Window x:Class="KMBEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KMBEditor"
        mc:Ignorable="d"
        Title="KMBEditor" Height="350" Width="525" WindowState="Maximized">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="380*"/>
            <ColumnDefinition Width="137*"/>
        </Grid.ColumnDefinitions>
        <Menu x:Name="menu" Grid.ColumnSpan="2" Margin="0,0,0,0.665" d:LayoutOverrides="LeftMargin, RightMargin, TopMargin, BottomMargin">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="新規作成(_N)"/>
                <MenuItem Header="開く(_O)" Command="{Binding OpenCommand}"/>
            </MenuItem>
        </Menu>
        <Button x:Name="prevButton" Content="Prev" Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,10" Grid.Row="1" VerticalAlignment="Bottom" Width="75" Command="{Binding PrevPageCommand}"/>
        <Button x:Name="nextButton" Content="Next" Grid.Column="1" HorizontalAlignment="Left" Margin="10,0,0,34.96" Grid.Row="1" VerticalAlignment="Bottom" Width="75" Command="{Binding NextPageCommand}"/>
        <StatusBar Grid.Row="2" Grid.ColumnSpan="2"/>
        <ToolBar x:Name="toolBar" Margin="0,0,10,0" Grid.Row="1" VerticalAlignment="Top" Grid.ColumnSpan="2" Height="9.666"/>
        <TabControl x:Name="tabControl" Margin="0,9.666,0,0.334" Grid.Row="1">
            <TabItem Header="TabItem">
                <Grid Background="#FFE5E5E5">
                    <TextBox x:Name="textBox" TextWrapping="Wrap" Text="{Binding AA.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Background="#FFD8D8D8" AcceptsReturn="True" FontFamily="MS PGothic"/>
                </Grid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
